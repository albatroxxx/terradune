name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Stable version to publish after validation
        required: true
        default: v1.0.0
  push:
    tags: ['v*']

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    uses: ./.github/workflows/ci.yml

  publish:
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      TAG: ${{ inputs.tag || github.ref_name }}
    permissions:
      contents: write
      packages: write
      id-token: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Verify stable release source
        id: version
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          test "$TAG" = "v$(cat VERSION)"
          git merge-base --is-ancestor HEAD origin/main
          test -f "docs/releases/$TAG.md"
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo 'This release already exists. Publish a new patch instead of overwriting it.'
            exit 1
          fi
          printf 'tag=%s\n' "$TAG" >> "$GITHUB_OUTPUT"
      - uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a # v4
      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4
      - uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: ghcr.io/albatroxxx/terradune
          tags: |
            type=semver,pattern={{version}},value=${{ steps.version.outputs.tag }}
            type=semver,pattern={{major}}.{{minor}},value=${{ steps.version.outputs.tag }}
            type=semver,pattern={{major}},value=${{ steps.version.outputs.tag }}
            type=raw,value=latest
      - id: image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: VERSION=${{ steps.version.outputs.tag }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          sbom: true
          provenance: mode=max
          cache-from: |
            type=gha,scope=amd64
            type=gha,scope=arm64
      - name: Verify anonymous image access before tagging the release
        env:
          DIGEST: ${{ steps.image.outputs.digest }}
        run: |
          anonymous_config=$(mktemp -d)
          trap 'rm -rf "$anonymous_config"' EXIT
          for attempt in $(seq 1 30); do
            if docker --config "$anonymous_config" pull "ghcr.io/albatroxxx/terradune@$DIGEST"; then
              exit 0
            fi
            echo 'Waiting for the package to be public in GitHub Packages settings.'
            sleep 10
          done
          echo 'Anonymous pull failed. Make the package public, then rerun the failed job.'
          exit 1
      - name: Smoke-test the published digest
        env:
          DIGEST: ${{ steps.image.outputs.digest }}
        run: bash scripts/docker-smoke.sh "ghcr.io/albatroxxx/terradune@$DIGEST"
      - name: Scan the published digest
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ghcr.io/albatroxxx/terradune@${{ steps.image.outputs.digest }}
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: '1'
          scanners: vuln
      - name: Publish release notes and immutable image reference
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DIGEST: ${{ steps.image.outputs.digest }}
        run: |
          cp "docs/releases/$TAG.md" "$RUNNER_TEMP/release.md"
          printf '\n## Verified Image\n\n`ghcr.io/albatroxxx/terradune@%s`\n\nIncludes Linux AMD64 and ARM64 images, SBOM, and provenance attestations.\n' "$DIGEST" >> "$RUNNER_TEMP/release.md"
          printf 'ghcr.io/albatroxxx/terradune@%s\n' "$DIGEST" > "$RUNNER_TEMP/container-digest.txt"
          gh release create "$TAG" --target "$GITHUB_SHA" --title "Terradune $TAG" --notes-file "$RUNNER_TEMP/release.md" "$RUNNER_TEMP/container-digest.txt"

  # The release already exists by now, with its notes and verified digest. This
  # adds the native downloads to it — the thing every OS package manager is
  # built on, and the only install path that needs no Go toolchain.
  binaries:
    name: Binaries
    needs: publish
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v7
        with:
          # GoReleaser reads tags and history to resolve the version.
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          check-latest: true
      - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
      - uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94 # v7.2.3
        with:
          version: '~> v2'
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # A workflow_dispatch run does not check out a tag ref, so the version
          # has to be named rather than inferred.
          GORELEASER_CURRENT_TAG: ${{ inputs.tag || github.ref_name }}
